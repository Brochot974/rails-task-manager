<h1><%= @task.title %></h1>

<h1><%= @task.details %></h1>

<%= link_to "index", tasks_path %>

<%= link_to "Delete Task",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
